package org.example.ProductServices.ProductCategories;

public class Keyboard extends Electronics {
    public Keyboard(String productName, int productPrice, boolean isSupported) {
        super(productName, productPrice, isSupported);
    }
}
